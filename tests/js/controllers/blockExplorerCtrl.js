
it.only('should work only if synced');
it.only('should fetch the latest block');
it.only('should fetch last 10 blocks and put them in an Array');
it.only('should print the number of transactions per block, size and pool?, timestamp');

//#sorting of blocks can be done in the ng-repeat directive with a filter